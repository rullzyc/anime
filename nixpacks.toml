[phases.setup]
nixPkgs = ["nodejs", "google-chrome", "vulkan-loader", "libGL"]

[phases.install]
cmds = ["cd backend && npm install"]

[phases.build]
cmds = ["cd backend && npm run build --if-present"]

[start]
cmd = "cd backend && node src/app.js"
